name: build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: endobit/ci/.github/workflows/go-build.yaml@v1
  lint:
    uses: endobit/ci/.github/workflows/go-lint.yaml@v1
